// Interfaces
import { ButtonProps } from '@components/Button/interfaces'

// Constants
export const DEFAULT_SHOW_BUTTON = false
export const DEFAULT_CUSTOM_TITLE: ButtonProps['customTitle'] = {
  className: 'hover:underline'
}
