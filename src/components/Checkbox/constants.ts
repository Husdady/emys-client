// Constants
export const PlaceholderProps = Object.freeze({
  DEFAULT_TOTAL_ROWS: 1
})

export const CheckboxProps = Object.freeze({
  DEFAULT_DISABLED: false,
  DEFAULT_LABEL: 'Checkbox',
  DEFAULT_CONTAINER_ELEMENT: 'div'
})
