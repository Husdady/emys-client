// Constants for Button component
export const ButtonProps = Object.freeze({
  DEFAULT_TYPE: 'button',
  DEFAULT_DISABLED: false,
  DEFAULT_LOADING_TITLE: 'Cargando...'
})

// COnstants for Spinner component
export const SpinnerProps = Object.freeze({
  DEFAULT_SIZE: 'w-4',
  DEFAULT_CENTER: true,
  DEFAULT_ALIGN: 'horizontal',
  DEFAULT_TITLE: 'Cargando...'
})
