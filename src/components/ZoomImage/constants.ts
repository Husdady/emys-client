// Interfaces
import { Offset } from './interfaces'

// Constants
export const DEFAULT_SCALE = 1.2
export const MIN_TARGET_WIDTH = 500
export const DEFAULT_TARGET_WIDTH = 1600
export const DEFAULT_OFFSET: Offset = { top: 0, left: 0 }
